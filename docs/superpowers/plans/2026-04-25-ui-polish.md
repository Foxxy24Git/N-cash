# UI/UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polish UI/UX N-Cash dengan loading states, empty states, error states, AlertDialog untuk delete bank, toast sukses ganti password, dan favicon branded — tanpa mengubah fitur atau logika bisnis.

**Architecture:** Buat shared `EmptyState` component, lalu buat file `loading.tsx` / `error.tsx` per-route (Next.js App Router convention), kemudian polish komponen satu per satu. Semua perubahan adalah UI-only.

**Tech Stack:** Next.js 14 App Router, TypeScript, Tailwind CSS, shadcn/ui (AlertDialog), Lucide icons, sonner (toast), next-auth/react (signOut)

---

## File Map

**Baru:**
- `src/components/ui/empty-state.tsx` — shared EmptyState component
- `src/app/(dashboard)/dashboard/loading.tsx` — skeleton dashboard
- `src/app/(dashboard)/dashboard/error.tsx` — error boundary dashboard
- `src/app/(dashboard)/reports/loading.tsx` — skeleton reports
- `src/app/(dashboard)/reports/error.tsx` — error boundary reports
- `src/app/icon.svg` — N-Cash branded favicon

**Diubah:**
- `src/app/(dashboard)/reports/_components/InvoiceTable.tsx` — empty state
- `src/app/(dashboard)/settings/_components/BankTab.tsx` — empty state
- `src/app/(dashboard)/settings/_components/UsersTab.tsx` — empty state
- `src/app/(dashboard)/settings/_components/DeleteBankButton.tsx` — window.confirm → AlertDialog
- `src/app/(dashboard)/settings/_actions/passwordActions.ts` — hapus signOut, return success
- `src/app/(dashboard)/settings/_components/PasswordTab.tsx` — toast sukses + client signOut

**Tidak berubah (sudah OK):**
- `src/app/(dashboard)/transactions/new/page.tsx` — sudah punya metadata title
- `src/app/(dashboard)/settings/page.tsx` — sudah punya metadata title
- Warna status badge — sudah konsisten di semua halaman

---

## Task 1: Shared EmptyState Component

**Files:**
- Create: `src/components/ui/empty-state.tsx`

- [ ] **Step 1: Buat file EmptyState**

```tsx
// src/components/ui/empty-state.tsx
import type { LucideIcon } from 'lucide-react'
import type { ReactNode } from 'react'

interface EmptyStateProps {
  icon: LucideIcon
  title: string
  subtitle?: string
  action?: ReactNode
}

export function EmptyState({ icon: Icon, title, subtitle, action }: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-16 text-center px-4">
      <Icon size={48} className="text-gray-300 mb-4" strokeWidth={1.5} />
      <p className="text-sm font-medium text-gray-500">{title}</p>
      {subtitle && <p className="text-xs text-gray-400 mt-1 max-w-xs">{subtitle}</p>}
      {action && <div className="mt-4">{action}</div>}
    </div>
  )
}
```

- [ ] **Step 2: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/empty-state.tsx
git commit -m "feat: add shared EmptyState component"
```

---

## Task 2: Dashboard — Loading & Error Boundaries

**Files:**
- Create: `src/app/(dashboard)/dashboard/loading.tsx`
- Create: `src/app/(dashboard)/dashboard/error.tsx`

- [ ] **Step 1: Buat loading.tsx**

```tsx
// src/app/(dashboard)/dashboard/loading.tsx
export default function DashboardLoading() {
  return (
    <div className="max-w-4xl mx-auto">
      <div className="h-7 w-48 bg-gray-200 rounded-md animate-pulse mb-6" />
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {Array.from({ length: 5 }).map((_, i) => (
          <div key={i} className="rounded-xl p-5 h-24 bg-gray-100 animate-pulse" />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Buat error.tsx**

```tsx
// src/app/(dashboard)/dashboard/error.tsx
'use client'

import { AlertTriangle } from 'lucide-react'

export default function DashboardError({
  reset,
}: {
  error: Error
  reset: () => void
}) {
  return (
    <div className="flex flex-col items-center justify-center py-20 text-center">
      <AlertTriangle size={40} className="text-red-400 mb-4" strokeWidth={1.5} />
      <p className="text-sm font-medium text-gray-700">Gagal memuat dashboard</p>
      <p className="text-xs text-gray-400 mt-1">Terjadi kesalahan saat mengambil data.</p>
      <button
        onClick={reset}
        className="mt-4 px-4 py-2 text-sm font-medium bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
      >
        Coba Lagi
      </button>
    </div>
  )
}
```

- [ ] **Step 3: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 4: Commit**

```bash
git add src/app/\(dashboard\)/dashboard/loading.tsx src/app/\(dashboard\)/dashboard/error.tsx
git commit -m "feat: add loading and error boundaries for dashboard"
```

---

## Task 3: Reports — Loading & Error Boundaries

**Files:**
- Create: `src/app/(dashboard)/reports/loading.tsx`
- Create: `src/app/(dashboard)/reports/error.tsx`

- [ ] **Step 1: Buat loading.tsx**

```tsx
// src/app/(dashboard)/reports/loading.tsx
export default function ReportsLoading() {
  return (
    <div className="max-w-6xl mx-auto space-y-5">
      {/* heading */}
      <div className="h-7 w-44 bg-gray-200 rounded-md animate-pulse" />

      {/* filter bar */}
      <div className="h-16 bg-gray-100 rounded-xl animate-pulse" />

      {/* export button */}
      <div className="flex justify-end">
        <div className="h-9 w-44 bg-gray-200 rounded-lg animate-pulse" />
      </div>

      {/* summary cards */}
      <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3">
        {Array.from({ length: 5 }).map((_, i) => (
          <div key={i} className="h-16 bg-gray-100 rounded-xl animate-pulse" />
        ))}
      </div>

      {/* table */}
      <div className="bg-white rounded-xl border border-gray-200 overflow-hidden">
        <div className="h-10 bg-gray-50 border-b border-gray-200" />
        {Array.from({ length: 8 }).map((_, i) => (
          <div key={i} className="h-12 border-b border-gray-100 px-4 flex items-center">
            <div
              className="h-4 bg-gray-100 rounded animate-pulse"
              style={{ width: `${50 + (i % 4) * 12}%` }}
            />
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Buat error.tsx**

```tsx
// src/app/(dashboard)/reports/error.tsx
'use client'

import { AlertTriangle } from 'lucide-react'

export default function ReportsError({
  reset,
}: {
  error: Error
  reset: () => void
}) {
  return (
    <div className="flex flex-col items-center justify-center py-20 text-center">
      <AlertTriangle size={40} className="text-red-400 mb-4" strokeWidth={1.5} />
      <p className="text-sm font-medium text-gray-700">Gagal memuat laporan</p>
      <p className="text-xs text-gray-400 mt-1">Terjadi kesalahan saat mengambil data.</p>
      <button
        onClick={reset}
        className="mt-4 px-4 py-2 text-sm font-medium bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
      >
        Coba Lagi
      </button>
    </div>
  )
}
```

- [ ] **Step 3: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 4: Commit**

```bash
git add src/app/\(dashboard\)/reports/loading.tsx src/app/\(dashboard\)/reports/error.tsx
git commit -m "feat: add loading and error boundaries for reports"
```

---

## Task 4: Apply EmptyState ke InvoiceTable, BankTab, UsersTab

**Files:**
- Modify: `src/app/(dashboard)/reports/_components/InvoiceTable.tsx`
- Modify: `src/app/(dashboard)/settings/_components/BankTab.tsx`
- Modify: `src/app/(dashboard)/settings/_components/UsersTab.tsx`

- [ ] **Step 1: Update InvoiceTable**

Di `src/app/(dashboard)/reports/_components/InvoiceTable.tsx`:

Tambah import di bagian atas:
```tsx
import { FileSearch } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'
```

Ganti block ini:
```tsx
{rows.length === 0 ? (
  <TableRow>
    <TableCell colSpan={7} className="text-center py-14 text-gray-400 text-sm">
      Tidak ada transaksi ditemukan untuk filter ini
    </TableCell>
  </TableRow>
) : (
```

Dengan:
```tsx
{rows.length === 0 ? (
  <TableRow>
    <TableCell colSpan={7} className="p-0">
      <EmptyState
        icon={FileSearch}
        title="Tidak ada transaksi"
        subtitle="Coba ubah filter atau rentang tanggal."
      />
    </TableCell>
  </TableRow>
) : (
```

- [ ] **Step 2: Update BankTab**

Di `src/app/(dashboard)/settings/_components/BankTab.tsx`:

Tambah import di bagian atas (Landmark sudah ada di lucide, tapi cek — jika belum, tambahkan):
```tsx
import { Plus, Landmark } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'
```

Ganti block ini:
```tsx
{banks.length === 0 ? (
  <div className="text-center py-12 text-gray-400 text-sm">
    Belum ada bank terdaftar. Tambah bank untuk digunakan pada transaksi Transfer Bank.
  </div>
) : (
```

Dengan:
```tsx
{banks.length === 0 ? (
  <EmptyState
    icon={Landmark}
    title="Belum ada bank"
    subtitle="Tambah bank untuk digunakan pada transaksi Transfer Bank."
  />
) : (
```

- [ ] **Step 3: Update UsersTab**

Di `src/app/(dashboard)/settings/_components/UsersTab.tsx`:

Ganti import lucide yang ada (`import { Plus } from 'lucide-react'`) dengan:
```tsx
import { Plus, Users } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'
```

Ganti block ini:
```tsx
{users.length === 0 ? (
  <div className="text-center py-12 text-gray-400 text-sm">
    Belum ada user terdaftar. Klik Tambah User.
  </div>
) : (
```

Dengan:
```tsx
{users.length === 0 ? (
  <EmptyState
    icon={Users}
    title="Belum ada pengguna lain"
    subtitle="Tambah pengguna untuk akses multi-user."
  />
) : (
```


- [ ] **Step 4: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(dashboard\)/reports/_components/InvoiceTable.tsx \
        src/app/\(dashboard\)/settings/_components/BankTab.tsx \
        src/app/\(dashboard\)/settings/_components/UsersTab.tsx
git commit -m "feat: replace text fallbacks with EmptyState component"
```

---

## Task 5: DeleteBankButton — window.confirm → AlertDialog

**Files:**
- Modify: `src/app/(dashboard)/settings/_components/DeleteBankButton.tsx`

- [ ] **Step 1: Tulis ulang DeleteBankButton**

Ganti seluruh isi file dengan:

```tsx
// src/app/(dashboard)/settings/_components/DeleteBankButton.tsx
'use client'

import { useState, useTransition } from 'react'
import { Button } from '@/components/ui/button'
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
} from '@/components/ui/alert-dialog'
import { toast } from 'sonner'
import { deleteBank } from '../_actions/bankActions'

export default function DeleteBankButton({ id }: { id: string }) {
  const [open, setOpen] = useState(false)
  const [isPending, startTransition] = useTransition()

  function handleDelete() {
    startTransition(async () => {
      const result = await deleteBank(id)
      if ('error' in result) {
        toast.error(result.error)
      } else {
        toast.success('Bank berhasil dihapus')
      }
      setOpen(false)
    })
  }

  return (
    <>
      <Button
        variant="ghost"
        size="sm"
        onClick={() => setOpen(true)}
        className="text-red-600 hover:text-red-700 hover:bg-red-50"
      >
        Hapus
      </Button>

      <AlertDialog open={open} onOpenChange={(v) => { if (!v) setOpen(false) }}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>Hapus bank ini?</AlertDialogTitle>
            <AlertDialogDescription>
              Tindakan ini tidak dapat dibatalkan.
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel disabled={isPending}>Batal</AlertDialogCancel>
            <AlertDialogAction
              onClick={handleDelete}
              disabled={isPending}
              className="bg-red-600 hover:bg-red-700 text-white focus:ring-red-600"
            >
              {isPending ? 'Menghapus...' : 'Ya, Hapus'}
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  )
}
```

- [ ] **Step 2: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 3: Commit**

```bash
git add src/app/\(dashboard\)/settings/_components/DeleteBankButton.tsx
git commit -m "fix: replace window.confirm with AlertDialog in DeleteBankButton"
```

---

## Task 6: Password Change — Toast Sukses + Client signOut

**Files:**
- Modify: `src/app/(dashboard)/settings/_actions/passwordActions.ts`
- Modify: `src/app/(dashboard)/settings/_components/PasswordTab.tsx`

- [ ] **Step 1: Update passwordActions.ts — hapus signOut, return success**

Ganti seluruh isi file dengan:

```ts
// src/app/(dashboard)/settings/_actions/passwordActions.ts
'use server'

import { prisma } from '@/lib/prisma'
import { auth } from '@/auth'
import bcrypt from 'bcryptjs'

type Result = { success: true } | { error: string }

export async function changePassword(formData: FormData): Promise<Result> {
  const session = await auth()
  if (!session?.user?.name) return { error: 'Tidak terautentikasi' }

  const oldPassword = formData.get('oldPassword') as string
  const newPassword = formData.get('newPassword') as string
  const confirmPassword = formData.get('confirmPassword') as string

  if (!oldPassword || !newPassword || !confirmPassword) {
    return { error: 'Semua field wajib diisi' }
  }
  if (newPassword.length < 8) {
    return { error: 'Password baru minimal 8 karakter' }
  }
  if (newPassword !== confirmPassword) {
    return { error: 'Konfirmasi password tidak cocok' }
  }
  if (newPassword.trim().length < 8) {
    return { error: 'Password tidak boleh hanya spasi' }
  }

  const user = await prisma.user.findUnique({ where: { username: session.user.name } })
  if (!user) return { error: 'User tidak ditemukan' }

  const match = await bcrypt.compare(oldPassword, user.password)
  if (!match) return { error: 'Password lama tidak cocok' }

  const hashed = await bcrypt.hash(newPassword, 10)
  await prisma.user.update({ where: { id: user.id }, data: { password: hashed } })

  return { success: true }
}
```

- [ ] **Step 2: Update PasswordTab.tsx — tambah toast + client signOut**

Ganti seluruh isi file dengan:

```tsx
// src/app/(dashboard)/settings/_components/PasswordTab.tsx
'use client'

import { useTransition } from 'react'
import { signOut } from 'next-auth/react'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { toast } from 'sonner'
import { changePassword } from '../_actions/passwordActions'

export default function PasswordTab() {
  const [isPending, startTransition] = useTransition()

  function handleSubmit(formData: FormData) {
    startTransition(async () => {
      const result = await changePassword(formData)
      if ('error' in result) {
        toast.error(result.error)
        return
      }
      toast.success('Password berhasil diubah. Logout dalam 1.5 detik...')
      setTimeout(() => signOut({ callbackUrl: '/login' }), 1500)
    })
  }

  return (
    <form action={handleSubmit} className="space-y-5 max-w-sm">
      <div className="space-y-1.5">
        <label htmlFor="oldPassword" className="text-sm font-medium text-gray-700">
          Password Lama
        </label>
        <Input id="oldPassword" name="oldPassword" type="password" required />
      </div>

      <div className="space-y-1.5">
        <label htmlFor="newPassword" className="text-sm font-medium text-gray-700">
          Password Baru{' '}
          <span className="text-xs font-normal text-gray-400">(min. 8 karakter)</span>
        </label>
        <Input id="newPassword" name="newPassword" type="password" required minLength={8} />
      </div>

      <div className="space-y-1.5">
        <label htmlFor="confirmPassword" className="text-sm font-medium text-gray-700">
          Konfirmasi Password Baru
        </label>
        <Input id="confirmPassword" name="confirmPassword" type="password" required />
      </div>

      <div className="space-y-3">
        <Button type="submit" disabled={isPending}>
          {isPending ? 'Menyimpan...' : 'Simpan Password'}
        </Button>
        <p className="text-xs text-gray-400">
          Setelah berhasil, Anda akan otomatis logout dan perlu login ulang.
        </p>
      </div>
    </form>
  )
}
```

- [ ] **Step 3: Type-check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 4: Commit**

```bash
git add src/app/\(dashboard\)/settings/_actions/passwordActions.ts \
        src/app/\(dashboard\)/settings/_components/PasswordTab.tsx
git commit -m "feat: add success toast for password change, move signOut to client"
```

---

## Task 7: Favicon N-Cash (icon.svg)

**Files:**
- Create: `src/app/icon.svg`

Next.js App Router secara otomatis menjadikan `src/app/icon.svg` sebagai favicon. File `src/app/favicon.ico` yang ada tetap sebagai fallback browser lama.

- [ ] **Step 1: Buat icon.svg**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <circle cx="16" cy="16" r="16" fill="#2563eb"/>
  <text
    x="16"
    y="22"
    font-family="Arial, Helvetica, sans-serif"
    font-size="18"
    font-weight="bold"
    fill="white"
    text-anchor="middle"
  >N</text>
</svg>
```

Simpan ke `src/app/icon.svg`.

- [ ] **Step 2: Build check**

```bash
cd /Users/user/N-Cash && npx tsc --noEmit 2>&1 | head -30
```

Expected: tidak ada error.

- [ ] **Step 3: Commit**

```bash
git add src/app/icon.svg
git commit -m "feat: add N-Cash branded SVG favicon"
```

---

## Task 8: Final Verification

- [ ] **Step 1: Start dev server**

```bash
cd /Users/user/N-Cash && npm run dev
```

- [ ] **Step 2: Cek tiap halaman**

Buka browser di `http://localhost:3000`:

| Halaman | Yang dicek |
|---------|-----------|
| `/dashboard` | Favicon biru "N" di tab browser; loading skeleton muncul saat refresh |
| `/reports` | Loading skeleton muncul saat refresh; empty state muncul jika filter kosong |
| `/settings?tab=banks` | Empty state muncul jika belum ada bank; delete pakai AlertDialog |
| `/settings?tab=company` | Toast sukses saat simpan |
| `/settings?tab=password` | Toast sukses muncul sebelum logout |
| `/settings?tab=users` | Empty state jika tidak ada user lain |

- [ ] **Step 3: Mobile check (375px)**

Di Chrome DevTools, set device ke "iPhone SE" (375px × 667px). Verifikasi:
- Bottom nav muncul, sidebar tersembunyi
- Form transaksi bisa di-scroll horizontal
- Table reports bisa di-scroll horizontal
- Cards dashboard stack vertikal

- [ ] **Step 4: Commit akhir jika ada perbaikan minor**

```bash
git add -p
git commit -m "fix: mobile responsive tweaks from final verification"
```
